# Текущая задача

## После фазы 13b (Studio Admin UI: детали + формы)

**Статус:** детальные страницы и HTML-формы под `/admin/*` (POST с PRG и flash `fs`/`fe`), вызовы тех же сервисов, что REST API; **без** новых сущностей, Memoh, бота, LLM/RAG.

**Следующий шаг:** по постановке — **13+** (дальнейший UX), **6+**, **14**, или доработки импорта / RAG.
