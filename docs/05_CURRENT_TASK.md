# Текущая задача

## После фазы 11a (assistant rules — storage / API / команды)

**Статус:** `studio_assistant_rules` + `studio_assistant_rule_audit`, API `/assistant-rules*`, команды `/rule_*` из control group; применение к LLM/Memoh — **не** в scope.

**Следующий шаг:** по постановке — применение правил к промпту/RAG, **6+**, **12–14**, или иной эпик.
