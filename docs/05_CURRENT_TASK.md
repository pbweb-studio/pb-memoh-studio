# Текущая задача

## После фазы 4b (вариант C)

**Статус:** минимальный транспорт Memoh → `POST /events/telegram` **реализован** (hook в Telegram adapter после дедупа, env-флаги, тесты).

**Сделано:** см. `docs/03_IMPLEMENTATION_PLAN.md` (раздел **Фаза 4b**), `docs/AI_CONTEXT.md`, `docs/04_PROJECT_LOG.md`.

**Следующий шаг:** при необходимости — e2e: поднять Studio (`docker-compose.local.yml`), включить зеркало в окружении Memoh, убедиться что сырые `Update` попадают в БД; далее планировать фазу 5+ без расширения scope 4b.

**Ограничение:** не расширять зеркало бизнес-правилами в Memoh; очередь ответов — только через существующие env Studio.
