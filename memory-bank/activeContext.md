# Active context

**Сейчас:** Фаза **4a** завершена (Event Mirror: `POST /events/telegram`, таблицы, нормализация, Alembic 002, тесты). Memoh и Telegram adapter не трогались. Дальше — **4b+** (транспорт в ingest) после ADR A/B/C; затем Фаза 5 (управляющая группа).

**Ветка:** `pb-studio/main`.

**Блокеры:** нет (нужно только решение по интеграции для подключения реального потока).
