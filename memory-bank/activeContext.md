# Active context

**Сейчас:** фаза **11b** в Studio — активные `assistant_rules` в user-prompt KB RAG (`/knowledge/ask`, `/kb_ask`), поле **`applied_rule_ids`**; опциональный **`chat_id`** в API; для `/kb_ask` — chat-контекст = control group `studio_chats.id`. **Без** Memoh, сводок, SLA, digest.

**Ветка:** `pb-studio/main`.

**Следующий шаг:** **6+** / **12+** или расширение KB — по постановке.
