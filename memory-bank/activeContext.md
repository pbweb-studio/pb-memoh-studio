# Active context

**Сейчас:** фаза **10d** в Studio — внешний KB embeddings: `STUDIO_KB_EMBEDDING_PROVIDER`, OpenAI-compatible `/embeddings`, батчи и redaction; deterministic для тестов. Memoh не менялся.

**Ветка:** `pb-studio/main`.

**Следующий шаг:** **6+** (LLM) или **10+** (Docling, полный RAG) — по отдельной постановке.
