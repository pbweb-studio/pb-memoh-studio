# Active context

**Сейчас:** фаза **6a** в Studio — `studio_chat_summaries`, планировщик pending jobs из Event Mirror, админ-API `/summaries*`, Celery `plan_daily_chat_summaries`. Без LLM, без отправки сводок в Telegram.

**Ветка:** `pb-studio/main`.

**Memoh:** не менялся в 6a.

**Следующий шаг:** фаза 6+ (генерация/продукт сводок) только по отдельной постановке.
